# SYSTEM ARCHITECTURE & DATA FLOW VISUALIZATION
## Excel AI ↔ Web UI Integration

---

## 🏗️ COMPLETE SYSTEM ARCHITECTURE

```
╔═══════════════════════════════════════════════════════════════════════╗
║                     AIRCRAFT LOAD PLANNING SYSTEM                      ║
║                        Complete Technology Stack                       ║
╚═══════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────┐
│                        LAYER 1: DATA INPUT                           │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ ┌───────────────────────────────────┐
         ├─│ User Manual Entry                 │
         │ │  • Position: A1, C2, F1...        │
         │ │  • ULD Type: LD-7, PMC, PAG...   │
         │ │  • Weight: 3200, 5800, 1400...   │
         │ │  • Destination: DXB, LHR, JFK... │
         │ └───────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    LAYER 2: AI KNOWLEDGE BASE                        │
│                          (Excel Sheet)                               │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ ┌───────────────────────────────────┐
         ├─│ ULD Master Table                  │
         │ │  Structure: ULD Type → Max Weight │
         │ │  Example:                         │
         │ │    LD-3  → 1588 kg               │
         │ │    LD-7  → 4600 kg               │
         │ │    PMC   → 6804 kg               │
         │ │    PAG   → 6000 kg               │
         │ │                                   │
         │ │  AI Function: Reference lookup    │
         │ └───────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   LAYER 3: AI INFERENCE ENGINE                       │
│                     (Excel Formulas - 139 Total)                     │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ ┌─────────────────────────────────────────────────┐
         ├─│ RULE 1: Weight Validation                       │
         │ │                                                 │
         │ │  =INDEX(Master!MaxWeight,                      │
         │ │         MATCH(Input!ULDType, Master!Type))     │
         │ │                                                 │
         │ │  IF (ActualWeight > MaxWeight)                 │
         │ │    THEN Status = "OVERLOAD"                    │
         │ │  ELSE IF (ActualWeight >= MaxWeight * 0.9)     │
         │ │    THEN Status = "NEAR LIMIT"                  │
         │ │  ELSE                                           │
         │ │    THEN Status = "SAFE"                        │
         │ └─────────────────────────────────────────────────┘
         │
         │ ┌─────────────────────────────────────────────────┐
         ├─│ RULE 2: Physics Calculation                     │
         │ │                                                 │
         │ │  FOR each cargo position:                       │
         │ │    Moment = Weight × Arm                        │
         │ │                                                 │
         │ │  TotalMoment = SUM(all moments)                │
         │ │  TotalWeight = SUM(all weights)                │
         │ │                                                 │
         │ │  CG = TotalMoment ÷ TotalWeight                │
         │ └─────────────────────────────────────────────────┘
         │
         │ ┌─────────────────────────────────────────────────┐
         └─│ RULE 3: Safety Decision                         │
           │                                                 │
           │  IF (CG < ForwardLimit)                        │
           │    THEN "⚠ FORWARD CG EXCEEDED"                │
           │  ELSE IF (CG > AftLimit)                       │
           │    THEN "⚠ AFT CG EXCEEDED"                    │
           │  ELSE                                           │
           │    THEN "✓ CG WITHIN SAFE ENVELOPE"            │
           │                                                 │
           │  IF (AnyOverload OR CGOutOfLimits)             │
           │    THEN "✖ NOT SAFE FOR FLIGHT"                │
           │  ELSE                                           │
           │    THEN "✓ SAFE FOR FLIGHT"                    │
           └─────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    LAYER 4: DATA PERSISTENCE                         │
│                        (Excel File Storage)                          │
└─────────────────────────────────────────────────────────────────────┘
         │
         │  aircraft_load_planning.xlsx
         │  ├─ ULD LOAD INPUT (user data)
         │  ├─ ULD MASTER TABLE (AI knowledge)
         │  ├─ CARGO HOLD VISUAL LAYOUT (AI analysis)
         │  ├─ ARM & MOMENT COMPUTATION (physics)
         │  ├─ CG & BALANCE DECISION ENGINE (AI decision)
         │  └─ AIRCRAFT LOAD & TRIM SHEET (final output)
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      LAYER 5: WEB INTERFACE                          │
│                     (React Application - UI)                         │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ ┌──────────────────────────────────────────────┐
         ├─│ FILE UPLOAD MODULE                           │
         │ │  • Drag & drop zone                         │
         │ │  • File validation (.xlsx only)             │
         │ │  • Sheet structure verification             │
         │ │  • Error handling with user feedback        │
         │ └──────────────────────────────────────────────┘
         │
         ↓
         │ ┌──────────────────────────────────────────────┐
         ├─│ DATA PARSING ENGINE (excelParser.js)         │
         │ │                                              │
         │ │  parseExcelData(workbook) {                 │
         │ │    // Extract sheets                        │
         │ │    uldInput = parseSheet('ULD LOAD INPUT')  │
         │ │    uldMaster = parseSheet('ULD MASTER')     │
         │ │    cgData = parseSheet('CG & BALANCE')      │
         │ │                                              │
         │ │    // Structure data                        │
         │ │    return {                                  │
         │ │      flightInfo: {...},                     │
         │ │      cargoPositions: [...],                 │
         │ │      cgAnalysis: {...},                     │
         │ │      visualStatus: {...}                    │
         │ │    }                                         │
         │ │  }                                           │
         │ └──────────────────────────────────────────────┘
         │
         ↓
         │ ┌──────────────────────────────────────────────┐
         ├─│ DASHBOARD RENDERER (React Components)        │
         │ │                                              │
         │ │  TrimSheetDashboard                         │
         │ │    ├─ Header (flight info)                  │
         │ │    ├─ SafetyStatusPanel                     │
         │ │    ├─ CargoVisualMap                        │
         │ │    ├─ CGEnvelopeChart                       │
         │ │    ├─ LoadSummaryPanel                      │
         │ │    └─ Footer (certification)                │
         │ └──────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      LAYER 6: VISUAL OUTPUT                          │
│                    (Interactive Dashboard UI)                        │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ ┌──────────────────────────────────────────────┐
         ├─│ CARGO VISUAL MAP                             │
         │ │                                              │
         │ │    ▲ NOSE                                    │
         │ │  ┌─────────────┐                            │
         │ │  │ A1    │ A2  │  🟢 Green = Safe          │
         │ │  ├─────────────┤  🟡 Yellow = Near Limit   │
         │ │  │ B1    │ B2  │  🔴 Red = Overload        │
         │ │  ├─────────────┤                            │
         │ │  │ C1    │ C2  │  Hover: Detailed tooltip  │
         │ │  └─────────────┘                            │
         │ │    ▼ TAIL                                    │
         │ └──────────────────────────────────────────────┘
         │
         │ ┌──────────────────────────────────────────────┐
         ├─│ CG ENVELOPE VISUALIZATION                    │
         │ │                                              │
         │ │  Forward │════════●════════│ Aft            │
         │ │   14.0m      20.11m       28.0m            │
         │ │           (Current CG)                      │
         │ │                                              │
         │ │  Status: ✓ CG WITHIN SAFE ENVELOPE          │
         │ └──────────────────────────────────────────────┘
         │
         │ ┌──────────────────────────────────────────────┐
         └─│ SAFETY DECISION PANEL                        │
           │                                              │
           │    ┌──────────────────────────────┐         │
           │    │   ✓ SAFE FOR FLIGHT          │         │
           │    │   (Large, Green Banner)      │         │
           │    └──────────────────────────────┘         │
           │                                              │
           │  Detailed Checks:                           │
           │  ✅ Weight Distribution OK                  │
           │  ✅ CG Position Safe                        │
           │  ✅ All ULDs Within Limits                  │
           └──────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                     LAYER 7: EXPORT & DELIVERY                       │
└─────────────────────────────────────────────────────────────────────┘
         │
         ├─ PDF Export (jsPDF + html2canvas)
         │    → aircraft_trim_sheet.pdf
         │
         └─ Print (browser print dialog)
              → Physical document for pilot briefing

═══════════════════════════════════════════════════════════════════════

                        DATA FLOW SUMMARY

  USER INPUT → EXCEL AI → WEB PARSER → UI RENDER → PDF OUTPUT
  (Manual)    (139 Rules) (JavaScript)  (React)    (Document)

═══════════════════════════════════════════════════════════════════════
```

---

## 🔄 DETAILED DATA FLOW EXAMPLE

### Example: Processing Position C2 (PMC, 6100 kg)

```
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 1: User Input (Excel ULD LOAD INPUT sheet)                     │
└─────────────────────────────────────────────────────────────────────┘

Row 8:  C2  |  PMC  |  6100  |  LHR

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 2: AI Knowledge Lookup (Excel ULD MASTER TABLE)                │
└─────────────────────────────────────────────────────────────────────┘

Formula: =INDEX(MasterTable!B:B, MATCH("PMC", MasterTable!A:A, 0))
Result:  MaxWeight = 6804 kg

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 3: AI Inference (Excel CARGO HOLD VISUAL LAYOUT)               │
└─────────────────────────────────────────────────────────────────────┘

Utilization = 6100 / 6804 = 0.896 = 89.6%

Status Logic:
IF 6100 > 6804 THEN "OVERLOAD"           → FALSE
ELSE IF 6100 >= 6804*0.9 THEN "NEAR LIMIT" → FALSE (89.6% < 90%)
ELSE "SAFE"                               → TRUE

Result: Status = "SAFE"

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 4: Web Parser Extraction (JavaScript excelParser.js)           │
└─────────────────────────────────────────────────────────────────────┘

const cargo = {
  position: "C2",
  uldType: "PMC",
  weight: 6100,
  destination: "LHR"
}

const status = {
  status: "SAFE",
  actualWeight: 6100,
  maxWeight: 6804,
  utilization: 0.896
}

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 5: React Component Rendering (CargoVisualMap.jsx)              │
└─────────────────────────────────────────────────────────────────────┘

<CargoCell
  positionId="C2"
  cargo={{ uldType: "PMC", weight: 6100 }}
  status={{ status: "SAFE", utilization: 0.896 }}
/>

CSS Applied:
  backgroundColor: #C6EFCE  (light green)
  borderColor: #4ADE80      (green)

Visual Result:
┌──────────────┐
│   POS C2     │  ← Gray header
├──────────────┤
│   PMC        │  ← Large text
│ 6,100 kg     │  ← Weight
├──────────────┤
│ ████████░░░░ │  ← 89.6% bar (green)
└──────────────┘
  ↑ Green background (SAFE status)

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 6: User Interaction (Hover Event)                              │
└─────────────────────────────────────────────────────────────────────┘

Tooltip Displays:
┌────────────────────────────┐
│ Position C2                │
│ ────────────────────────── │
│ ULD Type:    PMC           │
│ Actual Weight: 6,100 kg    │
│ Max Capacity:  6,804 kg    │
│ Deck:          Main        │
│ Utilization:   89.6%       │
│                            │
│ Status: SAFE ✅            │
│ Destination: LHR           │
└────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│ STEP 7: PDF Export (html2canvas + jsPDF)                            │
└─────────────────────────────────────────────────────────────────────┘

1. Capture dashboard as image (2x resolution)
2. Convert to canvas
3. Generate PDF page
4. Add C2 cell as part of cargo visual map
5. Save as aircraft_trim_sheet.pdf

Final Document:
  Page shows C2 as green cell in aircraft layout
  Includes all other positions
  Professional trim sheet format
```

---

## 🧠 AI DECISION VISUALIZATION MAPPING

### How Excel AI Logic Becomes Visual UI

```
╔═══════════════════════════════════════════════════════════════════╗
║              EXCEL AI FORMULA → WEB UI ELEMENT                     ║
╚═══════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────┐
│ AI DECISION 1: Weight Status Classification                       │
└───────────────────────────────────────────────────────────────────┘

Excel Formula (Cell F4):
═══════════════════════════════════════════════════════════════════
=IF(C4>D4, "OVERLOAD",
   IF(C4>=D4*0.9, "NEAR LIMIT", "SAFE"))
═══════════════════════════════════════════════════════════════════

Web UI Translation (CargoVisualMap.jsx):
═══════════════════════════════════════════════════════════════════
const getStatusColor = () => {
  switch (status.status) {
    case 'OVERLOAD':
      return 'bg-red-100 border-red-400';   // 🔴 RED
    case 'NEAR LIMIT':
      return 'bg-yellow-100 border-yellow-400'; // 🟡 YELLOW
    case 'SAFE':
      return 'bg-green-100 border-green-400';  // 🟢 GREEN
  }
}
═══════════════════════════════════════════════════════════════════

Visual Result:
  Excel "OVERLOAD" → Red cell background
  Excel "NEAR LIMIT" → Yellow cell background
  Excel "SAFE" → Green cell background

┌───────────────────────────────────────────────────────────────────┐
│ AI DECISION 2: CG Safety Evaluation                               │
└───────────────────────────────────────────────────────────────────┘

Excel Formula (Cell B19):
═══════════════════════════════════════════════════════════════════
=IF(B15<B7, "⚠ FORWARD CG LIMIT EXCEEDED",
   IF(B15>B8, "⚠ AFT CG LIMIT EXCEEDED",
      "✓ CG WITHIN SAFE ENVELOPE"))
═══════════════════════════════════════════════════════════════════

Web UI Translation (CGEnvelopeChart.jsx):
═══════════════════════════════════════════════════════════════════
const isSafe = cgStatus.includes('✓');

<div className={isSafe 
  ? 'bg-green-50 border-green-400' 
  : 'bg-red-50 border-red-400'}>
  {cgStatus}
</div>

// CG marker position
const cgPos = ((computedCG - forwardLimit) / totalRange) * 100;

<div style={{ left: `${cgPos}%` }}>
  <div className={isSafe ? 'bg-blue-600' : 'bg-red-600'} />
</div>
═══════════════════════════════════════════════════════════════════

Visual Result:
  Excel "✓ SAFE" → Green panel, blue CG marker
  Excel "⚠ EXCEEDED" → Red panel, red CG marker

┌───────────────────────────────────────────────────────────────────┐
│ AI DECISION 3: Overall Flight Safety                              │
└───────────────────────────────────────────────────────────────────┘

Excel Formula (Cell B21):
═══════════════════════════════════════════════════════════════════
=IF(COUNTIF(VisualLayout!F:F,"OVERLOAD")>0,
   "✖ NOT SAFE - OVERLOAD DETECTED",
   IF(OR(B15<B7,B15>B8),
      "✖ NOT SAFE - CG OUT OF LIMITS",
      "✓ SAFE FOR FLIGHT"))
═══════════════════════════════════════════════════════════════════

Web UI Translation (SafetyStatusPanel.jsx):
═══════════════════════════════════════════════════════════════════
const isSafe = cgAnalysis.isSafe && overloadCount === 0;

<div className={`
  ${isSafe 
    ? 'bg-green-600 text-white'    // ✅ SAFE
    : 'bg-red-600 text-white'      // ❌ NOT SAFE
  }
  text-2xl font-bold
`}>
  {status}
</div>

{isSafe ? (
  <CheckCircle className="w-16 h-16 text-green-600" />
) : (
  <XCircle className="w-16 h-16 text-red-600" />
)}
═══════════════════════════════════════════════════════════════════

Visual Result:
  Excel "✓ SAFE FOR FLIGHT" 
    → Large green banner
    → Green checkmark icon
    → "Aircraft cleared for departure" message

  Excel "✖ NOT SAFE"
    → Large red banner
    → Red X icon
    → "Load adjustment required" warning
```

---

## 📊 COMPONENT HIERARCHY

```
App
│
├─ FileUploadModule
│  ├─ DragDropZone
│  ├─ FileValidator
│  └─ StatusIndicator
│
└─ TrimSheetDashboard (rendered after upload)
   │
   ├─ Header
   │  ├─ FlightInfoPanel
   │  └─ SystemInfoPanel
   │
   ├─ SafetyStatusPanel ⭐ (Most prominent)
   │  ├─ StatusIcon (CheckCircle / XCircle)
   │  ├─ StatusBanner ("SAFE" / "NOT SAFE")
   │  └─ AlertMessage (if unsafe)
   │
   ├─ ContentGrid (2 columns)
   │  │
   │  ├─ LeftColumn
   │  │  ├─ CargoVisualMap
   │  │  │  ├─ AircraftNose
   │  │  │  ├─ CargoCell[12] (A1-F2)
   │  │  │  │  ├─ PositionLabel
   │  │  │  │  ├─ ULDTypeDisplay
   │  │  │  │  ├─ WeightDisplay
   │  │  │  │  ├─ UtilizationBar
   │  │  │  │  └─ HoverTooltip
   │  │  │  ├─ AircraftTail
   │  │  │  └─ Legend
   │  │  │
   │  │  └─ LoadSummaryPanel
   │  │     ├─ TotalWeightCard
   │  │     ├─ DeckDistribution
   │  │     ├─ ULDCount
   │  │     ├─ OverloadAlerts
   │  │     └─ DistributionChart
   │  │
   │  └─ RightColumn
   │     ├─ CGEnvelopeChart
   │     │  ├─ CGValueBoxes (3)
   │     │  ├─ EnvelopeBar
   │     │  │  ├─ SafeZone (green)
   │     │  │  ├─ CGMarker (blue/red)
   │     │  │  └─ LimitMarkers
   │     │  ├─ CGStatus
   │     │  ├─ TechnicalData
   │     │  └─ WarningMessages (if unsafe)
   │     │
   │     ├─ DetailedStatusChecks
   │     │  ├─ WeightCheck
   │     │  ├─ CGCheck
   │     │  └─ ULDCheck
   │     │
   │     └─ ULDStatisticsPanel
   │        ├─ TotalCount
   │        └─ TypeBreakdown[4]
   │
   └─ Footer
      ├─ CertificationInfo
      ├─ Timestamp
      └─ Disclaimer
```

---

## 🎯 CRITICAL SUCCESS FACTORS

### What Makes This System Work

**1. Seamless Integration**
```
Excel AI calculations → Web UI visualization
No data loss, no manual transfer, instant rendering
```

**2. Visual Intelligence**
```
Complex formulas → Intuitive colors
Rule-based logic → Clear status indicators
Binary decisions → Obvious go/no-go signals
```

**3. Professional Quality**
```
Matches real airline software
Operationally realistic
Print/PDF ready for actual use
```

**4. Complete Explainability**
```
Every color has a reason
Every status has a source
Every decision has an audit trail
```

**5. Zero Learning Curve**
```
Upload file → See results
No configuration, no setup
Instant operational readiness
```

---

**END OF ARCHITECTURE DOCUMENTATION**
